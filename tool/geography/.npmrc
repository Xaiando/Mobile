# The pipeline needs no native add-ons. mapshaper's optional GeoPackage and
# msgpack dependencies would compile with node-gyp; the build reads the
# GeoPackage with node:sqlite instead. npm run still runs the scripts.
ignore-scripts=true
